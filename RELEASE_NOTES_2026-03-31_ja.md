# リリースノート - 2026年3月31日

## 🎯 概要
このリリースでは、JAB証明書のスコープ読み込み・UI表示機能に関する重大な不具合の修正と、ユーザー体験向上のための規格マッチング判定表示の仕様変更が含まれています。

## ✨ 主な変更点

### 1. JABスコープ証明書のパース不具合の修正
- **施設データのパース上書きバグの解消**
  - **問題**: Netlify Functionの解析処理（`md.js`）において、JABスコープ証明書から複数の施設を読み取る際、施設を追加する処理がループの最後に1回しか記述されていなかったため、途中までに読み込んだ施設（施設1など）が全て上書き消滅し、一番最後の施設（施設2）しか表示されない不具合がありました。
  - **解決策**: 新しい施設データを発見したタイミングで逐次リスト（配列）に参照を保存し追加していくように処理を修正。これにより全ての施設が完全に表示されるようになりました。
- **施設ヘッダフォーマットの互換性確保**
  - 英語表記でのテスト時に発覚した構文解釈エラーを解消するため、バックエンドのパース処理と完全に整合性が取れるよう、MarkdownにおけるJABの施設ヘッダを、本来の日本語フォーマット（`## 【施設X】Name（Location）`）に統一して元に戻しました。

### 2. UI表示エラー（TypeError）の解消
- **規格データカテゴリ仕分け時のバグ修正**
  - **問題**: JAB証明書のデータ読み込み処理（`script.js`）内で、バックエンドから受け取った規格データを施設カテゴリへ振り分ける際、存在しないプロパティ（`standard_number`）を参照していたため、undefinedエラー（`TypeError: Cannot read properties of undefined (reading 'standard')`）となり、後続のリスト描画処理全体がクラッシュして画面が真っ白（または更新停止）になる致命的な不具合がありました。
  - **解決策**: 抽出した規格オブジェクトそのものをそのまま配列に格納するようプログラムを修正。正しく描画処理へデータが引き渡されるようになりました。
- **不要なハイフン（-）の抑止**
  - 規格の説明文が存在しない（空の）場合、画面表示の末尾に「-」が不要に表示されてしまう仕様（例：`IEC 60945 -`）を改善し、説明がない場合は規格名のみがスッキリと表示されるように修正しました。

### 3. ISO17025バッジ判定表示の仕様変更（緑統一）
- **表記揺れ（表記違い）の扱いを緑判定に変更**
  - これまで「完全一致」のみ緑色のチェックマーク（🟢）で表示し、「包括スコープ（comprehensive_match）」「バージョン許容（version_tolerant_match）」「前置き違い（prefix_mismatch）」「バージョン違い（version_mismatch）」などの表記揺れについては、黄色（🟡）やオレンジ（🟠）などの警告色バッジやアイコンが表示されていました。
  - 本リリースより、これら「表記違い等による合致」について、すべて完全一致と同じ**「緑色のチェックマーク（🟢）」および「緑色バッジ」**として統一表示するよう仕様を変更しました。
- **注意書きテキストの保持**
  - バッジ自体の色は変わりますが、内部で判定された『⚠️ 表記違い(...)』などの詳細なテキスト補足は引き続き横に表示され、後から原因を確認できるようになっています。
  - スコープから外れているもの（no_match / 赤・黒系）の表示はこれまで通り維持されています。

## 📝 技術詳細

### 変更されたファイル

#### `static/data/jab-scopes.md`
- 施設ヘッダのフォーマットを【施設X】を用いた標準フォーマットへ再統一し、既存の正規表現パーサーに適合させました。

#### `netlify/functions/utils/md.js`
- `parseCertificateMD` 関数内のJAB施設パース処理を修正。
  - `currentFacility` が構築された直後に `certificateData.facilities` へpushするよう書き換え。後からの参照追加（`standards.push`）が自動的に全体ツリーに反映される仕組みを活かしました。

#### `static/script.js`
- `loadCertificateData` 呼び出し後のUI生成部：
  - `facilityCategories[category].push(standard.standard_number)` を `facilityCategories[category].push(standard)` へ置換し、描画エラーを解決。
- 規格テキストのハイフンサフィックス表示ロジックを修正。
- `createScopeBadge` および `createScopeSearchResult` 内の「表記揺れ」判別ケース（`prefix_mismatch`, `version_mismatch`等）において、アイコンクラスを全て `fa-check-circle`、およびステータス表象を `🟢` へ書き換え。

#### `static/style.css`
- `.scope-badge.prefix-mismatch`, `.version-mismatch` などのCSS設定を改修。背景色を `#d4edda`、文字色を `#155724` に設定し、`exact-match`クラスの緑色デザインと完全に揃えました。
- `.scope-search-match` の各ミスマッチ要素にも同様の緑枠デザインを適用。

## 🚀 デプロイメント注意事項
- すべての変更はデプロイ後すぐに有効になります。API仕様の破壊的変更はないため、過去にキャッシュされた状態への影響もありません。
